# memorygame
A fairly small and simple memory game I made using vanilla JS with no frameworks or libraries. 

The game creates a new set of matching squares when reset is clicked, so you'll get a new game.
